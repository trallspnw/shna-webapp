# Database connection string (Postgres only)
DATABASE_URL=postgresql://payload:payload@localhost:5432/payload

# Production database connection string (for local sync only)
PROD_DATABASE_URL=postgresql://postgres.<project-ref>:[YOUR-PASSWORD]@aws-0-<region>.pooler.supabase.com:5432/postgres

# Used to encrypt JWT tokens
PAYLOAD_SECRET=YOUR_SECRET_HERE

# CMS base URL (used for CORS, links, admin preview, and static export). No trailing slash
NEXT_PUBLIC_CMS_URL=http://localhost:3000

# Public site base URL (static export). No trailing slash
NEXT_PUBLIC_SITE_URL=http://localhost:3001

# Cloudflare R2 (media storage)
R2_BUCKET=shna
R2_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
R2_PUBLIC_URL=https://<public-media-domain>
R2_REGION=auto
R2_ACCESS_KEY_ID=YOUR_R2_ACCESS_KEY_ID
R2_SECRET_ACCESS_KEY=YOUR_R2_SECRET_ACCESS_KEY
R2_PREFIX=local
# Public media origin (no trailing slash)
NEXT_PUBLIC_MEDIA_ORIGIN=https://<public-media-domain>

# Brevo (email + list sync)
BREVO_API_KEY=
BREVO_SENDER_EMAIL=noreply@seminaryhillnaturalarea.org

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOKS_ENDPOINT_SECRET=whsec_...

# Secret used to authenticate cron jobs
CRON_SECRET=YOUR_CRON_SECRET_HERE

# Used to validate preview requests
PREVIEW_SECRET=YOUR_SECRET_HERE
