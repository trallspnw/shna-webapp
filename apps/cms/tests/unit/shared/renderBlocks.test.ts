import { describe, it } from 'vitest'

describe('RenderBlocks', () => {
  // TODO(followup): Fix @payloadcms/ui SCSS imports in Vitest and re-enable.
  it.skip('[future] renders membership, donation, and subscription blocks', () => {
    // Intentionally skipped until SCSS imports are supported in Vitest.
  })
})
